# Index Operations: Index-based tensor operations
# Source: framework/src/interface/operation/opcode.cpp
#   RegisterVector()  OP_INDEX_OUTCAST (line 547), OP_INDEX_ADD (line 531)
#   RegisterVector()  OP_INDEX_PUT (line 500)

OP_INDEX_OUTCAST:
  description: "索引散射输出（根据索引将UB数据写出到GM指定位置）"
  calc_type: MOVE_OUT
  core_type: ANY
  tileop: "TileOp::TIndexoutcast"
  pipe: [PIPE_MTE3, PIPE_MTE3]
  dtype:
    fp16: supported
    bf16: supported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [UB, UB, GM]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [GM]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
  tile_shape:
    api: null
    constraints: []
    recommended: []
  tags: [SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 根据索引将数据从UB写出到GM指定位置
    - 使用MTE3流水线搬出数据
    - 属于IsCopyOut集合
    - 属于IsOpCodeSupportMultiProducers集合（支持多生产者）
    - 有axis、inputCombineAxis、cacheMode、panzBlockSize属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:547 RegisterVector"

OP_INDEX_PUT:
  description: "索引写入（根据索引将数据写入目标张量指定位置，GM到GM）"
  calc_type: MOVE_OUT
  core_type: ANY
  tileop: "TileOp::TIndexPut"
  pipe: [PIPE_MTE3, PIPE_MTE3]
  dtype:
    fp16: supported
    bf16: supported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [GM, UB, UB, UB, UB, UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [GM]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
  tile_shape:
    api: null
    constraints: []
    recommended: []
  tags: [INDEX_PUT_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 根据索引将数据写入目标张量指定位置
    - 输入源和输出都在GM上
    - 使用MTE3流水线搬出数据
    - 属于IsCopyOut集合
    - 有accumulate、indicesSize属性
    - 无TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:500 RegisterVector"

OP_INDEX_ADD:
  description: "索引累加（根据索引将数据累加到目标张量指定位置）"
  calc_type: OTHER
  core_type: AIV
  tileop: "TileOp::TindexAdd"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: supported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [UB, UB, UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB, UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [INDEX_ADD_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 在UB中执行索引累加操作
    - 有axis、scalar属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:531 RegisterVector"

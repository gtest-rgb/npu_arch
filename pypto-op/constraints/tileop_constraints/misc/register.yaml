# Register Copy Operation: Register-level copy
# Source: framework/src/interface/operation/opcode.cpp
#   RegisterCommon()  OP_REGISTER_COPY (line 794)

OP_REGISTER_COPY:
  description: "寄存器间数据复制（Vector寄存器复制操作）"
  calc_type: SYS
  core_type: AIV
  tileop: "REGISTER_COPY"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: supported
    fp32: supported
    int8: supported
  operands:
    input:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
  tile_shape:
    api: null
    constraints: []
    recommended: []
  tags: []
  notes: |
    - 寄存器级复制，不经过UB
    - 有TileShapeVerifier验证
    - calc_type为SYS，表示系统级操作
    - 不需要set_vec_tile_shapes
  source: "opcode.cpp:794 RegisterCommon"

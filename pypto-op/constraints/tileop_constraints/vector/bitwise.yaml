# Bitwise Operations: AND, OR, XOR, NOT, LEFTSHIFT, RIGHTSHIFT (tensor-tensor & tensor-scalar)
# Source: framework/src/interface/operation/opcode.cpp
#   RegisterVectorBinary()  OP_BITWISERIGHTSHIFT (line 88), OP_BITWISELEFTSHIFT (line 91),
#                           OP_BITWISEAND (line 94), OP_BITWISEOR (line 97),
#                           OP_BITWISEXOR (line 100)
#   RegisterVectorUnary()   OP_BITWISENOT (line 284)
#   RegisterVectorBinary()  OP_BITWISERIGHTSHIFTS (line 160), OP_BITWISELEFTSHIFTS (line 165),
#                           OP_BITWISEANDS (line 178), OP_BITWISEORS (line 183),
#                           OP_BITWISEXORS (line 188)

# =============================================================================
# Bitwise Binary Operations (tensor-tensor, calc_type=BROADCAST)
# =============================================================================

OP_BITWISERIGHTSHIFT:
  description: "张量逐元素按位右移（tensor-tensor）"
  calc_type: BROADCAST
  core_type: AIV
  tileop: "TileOp::Tbitwiserightshift"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input_a:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    input_b:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [BINARY_OPS, UNSUPPORT_BF16_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 支持inputCombineAxis属性
    - 有excludeBufferReuse标签，输出缓冲区不可复用
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:88 RegisterVectorBinary"

OP_BITWISELEFTSHIFT:
  description: "张量逐元素按位左移（tensor-tensor）"
  calc_type: BROADCAST
  core_type: AIV
  tileop: "TileOp::Tbitwiseleftshift"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input_a:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    input_b:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [BINARY_OPS, UNSUPPORT_BF16_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 支持inputCombineAxis属性
    - 有excludeBufferReuse标签，输出缓冲区不可复用
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:91 RegisterVectorBinary"

OP_BITWISEAND:
  description: "张量逐元素按位与（tensor-tensor）"
  calc_type: BROADCAST
  core_type: AIV
  tileop: "TileOp::TbitwiseAnd"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input_a:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    input_b:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [BINARY_OPS, UNSUPPORT_BF16_OPS, LASTUSE_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 支持inputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:94 RegisterVectorBinary"

OP_BITWISEOR:
  description: "张量逐元素按位或（tensor-tensor）"
  calc_type: BROADCAST
  core_type: AIV
  tileop: "TileOp::TbitwiseOr"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input_a:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    input_b:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [BINARY_OPS, UNSUPPORT_BF16_OPS, LASTUSE_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 支持inputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:97 RegisterVectorBinary"

OP_BITWISEXOR:
  description: "张量逐元素按位异或（tensor-tensor）"
  calc_type: BROADCAST
  core_type: AIV
  tileop: "TileOp::TbitwiseXor"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input_a:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    input_b:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [BINARY_OPS, UNSUPPORT_BF16_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 支持inputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:100 RegisterVectorBinary"

# =============================================================================
# Bitwise NOT (unary, calc_type=ELMWISE)
# =============================================================================

OP_BITWISENOT:
  description: "张量逐元素按位取反"
  calc_type: ELMWISE
  core_type: AIV
  tileop: "TileOp::Tbitwisenot"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [UNARY_OPS, UNSUPPORT_BF16_OPS, LASTUSE_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 支持inputCombineAxis和outputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:284 RegisterVectorUnary"

# =============================================================================
# Bitwise Scalar Operations (tensor-scalar, calc_type=ELMWISE)
# =============================================================================

OP_BITWISERIGHTSHIFTS:
  description: "张量与标量逐元素按位右移（tensor-scalar）"
  calc_type: ELMWISE
  core_type: AIV
  tileop: "TileOp::Tbitwiserightshifts"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [VECTOR_SCALAR_OPS, UNSUPPORT_BF16_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 有scalar属性传入标量值
    - 有reverseOperand属性控制操作数顺序
    - 支持inputCombineAxis和outputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:160 RegisterVectorBinary"

OP_BITWISELEFTSHIFTS:
  description: "张量与标量逐元素按位左移（tensor-scalar）"
  calc_type: ELMWISE
  core_type: AIV
  tileop: "TileOp::Tbitwiseleftshifts"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [VECTOR_SCALAR_OPS, UNSUPPORT_BF16_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 有scalar属性传入标量值
    - 有reverseOperand属性控制操作数顺序
    - 支持inputCombineAxis和outputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:165 RegisterVectorBinary"

OP_BITWISEANDS:
  description: "张量与标量逐元素按位与（tensor-scalar）"
  calc_type: ELMWISE
  core_type: AIV
  tileop: "TileOp::Tbitwiseands"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [VECTOR_SCALAR_OPS, UNSUPPORT_BF16_OPS, LASTUSE_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 有scalar属性传入标量值
    - 有reverseOperand属性控制操作数顺序
    - 有excludeBufferReuse标签，输出缓冲区不可复用
    - 支持inputCombineAxis和outputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:178 RegisterVectorBinary"

OP_BITWISEORS:
  description: "张量与标量逐元素按位或（tensor-scalar）"
  calc_type: ELMWISE
  core_type: AIV
  tileop: "TileOp::Tbitwiseors"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [VECTOR_SCALAR_OPS, UNSUPPORT_BF16_OPS, LASTUSE_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 有scalar属性传入标量值
    - 有reverseOperand属性控制操作数顺序
    - 有excludeBufferReuse标签，输出缓冲区不可复用
    - 支持inputCombineAxis和outputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:183 RegisterVectorBinary"

OP_BITWISEXORS:
  description: "张量与标量逐元素按位异或（tensor-scalar）"
  calc_type: ELMWISE
  core_type: AIV
  tileop: "TileOp::Tbitwisexors"
  pipe: [PIPE_V, PIPE_V]
  dtype:
    fp16: supported
    bf16: unsupported
    fp32: supported
    int8: null
  operands:
    input:
      mem_type: [UB]
      layout:
        type: "任意"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
    output:
      mem_type: [UB]
      layout:
        type: "与输入一致"
        contiguous: false
        max_dims: 5
      stride:
        required: false
        constraints: []
      alignment:
        address: null
        scope: null
      shape:
        min_elements: 1
        max_elements_per_dim: null
  hardware:
    mask_len: 64
    repeat_max: 255
    block_size: 32
    elements_per_block:
      fp16: 16
      bf16: 16
      fp32: 8
  tile_shape:
    api: "set_vec_tile_shapes"
    constraints:
      - rule: "末尾轴大小 * element_size % 32 == 0"
        severity: error
        tileop_cause: "Vector指令要求每次读取的字节数为32的整数倍"
    recommended:
      - scenario: "通常计算"
        value: "末尾轴对齐到256Bytes"
        reasoning: "充分利用UB带宽"
  tags: [VECTOR_SCALAR_OPS, UNSUPPORT_BF16_OPS, SUPPORT_DYNAMIC_UNALIGNED_OPS]
  notes: |
    - 有scalar属性传入标量值
    - 有reverseOperand属性控制操作数顺序
    - 有excludeBufferReuse标签，输出缓冲区不可复用
    - 支持inputCombineAxis和outputCombineAxis属性
    - 有TileShapeVerifier验证
    - 属于SUPPORT_DYNAMIC_UNALIGNED_OPS
  source: "opcode.cpp:188 RegisterVectorBinary"
